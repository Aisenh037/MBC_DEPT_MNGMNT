import express from 'express';
const router = express.Router();

// 🔁 Replace the below example handlers with your actual logic
router.get('/', (req, res) => res.send('Courses endpoint'));

export default router;
