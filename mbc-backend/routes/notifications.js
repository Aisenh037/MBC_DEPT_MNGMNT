import express from 'express';

const router = express.Router();

// 🔁 Replace with actual handler logic
router.get('/', (req, res) => res.send('Notifications endpoint'));

export default router;
