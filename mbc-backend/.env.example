# .env.example
# Copy this file to .env.development for local work.
# These values will be set directly in the hosting provider (e.g., Render) for production.

# ----------------------------------
# ENVIRONMENT
# ----------------------------------
NODE_ENV=development

# ----------------------------------
# SERVER & DATABASE
# ----------------------------------
PORT=5000
MONGO_URI=
CORS_ORIGIN=http://localhost:5173

# ----------------------------------
# AUTHENTICATION & SECURITY
# ----------------------------------
# ✨ IMPORTANT: Generate a strong, random string for JWT_SECRET
JWT_SECRET=
JWT_EXPIRE=30d

# ----------------------------------
# SEEDING
# ----------------------------------
SEED_ADMIN=true
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_this_strong_password

# ----------------------------------
# EMAIL SERVICE (e.g., Mailtrap for dev, SendGrid for prod)
# ----------------------------------
FROM_EMAIL=donotreply@example.com
FROM_NAME="MBC Department Portal"
# Add your specific email provider keys here (e.g., SENDGRID_API_KEY=)